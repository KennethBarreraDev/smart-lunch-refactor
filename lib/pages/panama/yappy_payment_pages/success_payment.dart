import 'package:flutter/material.dart';

class YappyPaymentSuccess extends StatelessWidget {
  const YappyPaymentSuccess({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Exito")),
    );
  }
}
